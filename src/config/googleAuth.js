// Your Google Sign-In "Client ID" goes here — see
// data-sourcing/GOOGLE-SIGNIN-SETUP.md for exactly how to get one (about 5
// minutes, no coding involved).
//
// This is a public identifier, not a secret. It's visible in every browser
// request the sign-in flow makes — every site using "Sign in with Google"
// exposes it the same way — so it's safe to commit straight into the repo
// rather than hiding it in an environment variable or a GitHub secret.
//
// Until you replace the placeholder below, the "Sign in with Google" button
// won't appear — the rest of sign-in (the name/email form, the guest demo)
// keeps working exactly as before.

export const GOOGLE_CLIENT_ID = '840569465709-fn8lfr64q4pts7ucnj6diak4357uvv8d.apps.googleusercontent.com'
