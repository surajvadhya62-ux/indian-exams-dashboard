# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vite
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
dist.zip
site.zip
scripts/automation/*.json
# Except the checked-in template for notify-config.json (real credentials, gitignored above).
!scripts/automation/notify-config.example.json

# Portal watch working state. The snapshots are regenerable machine state and churn on every
# run; the permanent record is data-sourcing/PORTAL-CHANGE-LOG.md, which IS tracked.
data-sourcing/portal-snapshots/

# Antigravity verification screenshots. Local evidence for the human screenshot-check step;
# the permanent record is the written confidence/source_url in each dossier and the row in
# VERIFICATION-QUEUE.md, not the image itself.
data-sourcing/screenshots/
